services:
  web:
    build: .
    restart: unless-stopped
    init: true
    ports:
      - "8585:8080"
    environment:
      DATA_DIR: /data
      PORT: 8080
      HOME_ASSISTANT_TOKEN: ${HOME_ASSISTANT_TOKEN:-}
    volumes:
      - app-data:/data

volumes:
  app-data:
